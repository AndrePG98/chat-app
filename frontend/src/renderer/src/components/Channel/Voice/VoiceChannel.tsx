import { ChannelDTO } from "../../../DTOs/ChannelDTO"
import React from "react"

export default function VoiceChannel(props: { channnel: ChannelDTO }) {
	return <div className="voice-channel flex-1">VoiceChannel</div>
}
